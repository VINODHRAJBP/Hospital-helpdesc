export interface book {
    id: string,
    name: string,
    author: string,
    price: number,
    count: number,
    image: string
}

export interface admin{
    email:string,
    password:string
}